package com.fiscal.compass.presentation.utils

/**
 * Enum to distinguish between different types of amount inputs
 * in the add transaction screen.
 */
enum class AmountInputType {
    TOTAL_AMOUNT,
    AMOUNT_PAID
}
